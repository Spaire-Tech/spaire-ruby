# Emoji

## Example Usage

```ruby
require "spaire"

value = Emoji::THUMBSUP

# Open enum: use .deserialize() to create instances from custom string values
custom = Emoji.deserialize("custom_value")
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `THUMBSUP` | thumbsup   |
| `CLAP`     | clap       |
| `HEART`    | heart      |
| `FIRE`     | fire       |
| `IDEA`     | idea       |
| `PRAY`     | pray       |