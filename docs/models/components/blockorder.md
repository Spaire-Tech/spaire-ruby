# BlockOrder

## Example Usage

```ruby
require "spaire"

value = BlockOrder::PRODUCTS

# Open enum: use .deserialize() to create instances from custom string values
custom = BlockOrder.deserialize("custom_value")
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `PRODUCTS` | products   |
| `LINKS`    | links      |
| `FORMS`    | forms      |