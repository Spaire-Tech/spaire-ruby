# SeatTierType

## Example Usage

```ruby
require "spaire"

value = SeatTierType::VOLUME

# Open enum: use .deserialize() to create instances from custom string values
custom = SeatTierType.deserialize("custom_value")
```


## Values

| Name        | Value       |
| ----------- | ----------- |
| `VOLUME`    | volume      |
| `GRADUATED` | graduated   |