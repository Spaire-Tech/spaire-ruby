# BenefitGrantGitHubRepositoryPropertiesPermission

## Example Usage

```ruby
require "spaire"

value = BenefitGrantGitHubRepositoryPropertiesPermission::PULL

# Open enum: use .deserialize() to create instances from custom string values
custom = BenefitGrantGitHubRepositoryPropertiesPermission.deserialize("custom_value")
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `PULL`     | pull       |
| `TRIAGE`   | triage     |
| `PUSH`     | push       |
| `MAINTAIN` | maintain   |
| `ADMIN`    | admin      |