# CommunityPostEventRefType

## Example Usage

```ruby
require "spaire"

value = CommunityPostEventRefType::WORKSHOP

# Open enum: use .deserialize() to create instances from custom string values
custom = CommunityPostEventRefType.deserialize("custom_value")
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `WORKSHOP` | workshop   |
| `OFFICE`   | office     |
| `COHORT`   | cohort     |
| `GUEST`    | guest      |