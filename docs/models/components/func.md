# Func

## Example Usage

```ruby
require "spaire"

value = Func::SUM

# Open enum: use .deserialize() to create instances from custom string values
custom = Func.deserialize("custom_value")
```


## Values

| Name  | Value |
| ----- | ----- |
| `SUM` | sum   |
| `MAX` | max   |
| `MIN` | min   |
| `AVG` | avg   |