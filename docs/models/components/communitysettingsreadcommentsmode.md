# CommunitySettingsReadCommentsMode

## Example Usage

```ruby
require "spaire"

value = CommunitySettingsReadCommentsMode::VISIBLE

# Open enum: use .deserialize() to create instances from custom string values
custom = CommunitySettingsReadCommentsMode.deserialize("custom_value")
```


## Values

| Name      | Value     |
| --------- | --------- |
| `VISIBLE` | visible   |
| `HIDDEN`  | hidden    |
| `LOCKED`  | locked    |