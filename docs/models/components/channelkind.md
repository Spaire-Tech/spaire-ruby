# ChannelKind

## Example Usage

```ruby
require "spaire"

value = ChannelKind::MODULE

# Open enum: use .deserialize() to create instances from custom string values
custom = ChannelKind.deserialize("custom_value")
```


## Values

| Name     | Value    |
| -------- | -------- |
| `MODULE` | module   |
| `LESSON` | lesson   |