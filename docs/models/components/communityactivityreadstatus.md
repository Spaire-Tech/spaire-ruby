# CommunityActivityReadStatus

## Example Usage

```ruby
require "spaire"

value = CommunityActivityReadStatus::OPEN

# Open enum: use .deserialize() to create instances from custom string values
custom = CommunityActivityReadStatus.deserialize("custom_value")
```


## Values

| Name     | Value    |
| -------- | -------- |
| `OPEN`   | open     |
| `CLOSED` | closed   |