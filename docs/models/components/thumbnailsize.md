# ThumbnailSize

Product thumbnail size

## Example Usage

```ruby
require "spaire"

value = ThumbnailSize::SMALL

# Open enum: use .deserialize() to create instances from custom string values
custom = ThumbnailSize.deserialize("custom_value")
```


## Values

| Name     | Value    |
| -------- | -------- |
| `SMALL`  | small    |
| `MEDIUM` | medium   |
| `LARGE`  | large    |